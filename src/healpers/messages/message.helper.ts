const PASSWORD_INCORRET =
  'A senha deve conter letaras maiúscialas, minúsculas, números e carateres especiais';

const INVALID_PASSWORD_LENGTH = 'A senha deve ter mais de 3 carateres';

const USER_NOT_FOUND = 'Usuário não encontrado ou não existe.';

const CITY_NOT_FOUND = 'Cidade não encontrado ou não existe.';

const EMAIL_AND_PASSWOR_INVALID = 'E-mail ou senha inválidos';

const CART_NOT_ACTIVE = 'Não há carrinho ativo';

const ORDER_NOT_FOUND = 'Pedido não encontrado ou não existe.';

const PAYMENT_INVALID =
  'Pagamentos de valor ou código pix ou data de pagamento não encontrado';

const PRODUCT_NOT_FOUND = 'Producto não encontrado ou não existe.';

const LAST_PASSWORD_INVALID = 'Última senha inválida';

const PASSWORD_FALL = 'A senha inválida';

const CPF_EXIST = 'CPF já cadastrado no sistema';

export const HelperMessage = {
  PASSWORD_INCORRET,
  INVALID_PASSWORD_LENGTH,
  USER_NOT_FOUND,
  CITY_NOT_FOUND,
  EMAIL_AND_PASSWOR_INVALID,
  CART_NOT_ACTIVE,
  ORDER_NOT_FOUND,
  PAYMENT_INVALID,
  PRODUCT_NOT_FOUND,
  LAST_PASSWORD_INVALID,
  PASSWORD_FALL,
  CPF_EXIST,
};
